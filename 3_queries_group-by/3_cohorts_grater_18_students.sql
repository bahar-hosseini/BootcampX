SELECT count(students.name) as student_name, cohorts.name as cohort_name
FROM cohorts INNER JOIN students ON cohorts.id = cohort_id GROUP BY cohort_name HAVING count(students.name) >= 18 ORDER BY student_name;